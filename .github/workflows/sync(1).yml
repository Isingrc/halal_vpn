name: Sync VPN Configs

on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Fetch, ping-sort and build config files
        run: |
          python3 << 'EOF'
          import urllib.request
          import re
          import subprocess
          from datetime import datetime

          BASE = "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/main/"
          DATE = datetime.utcnow().strftime("%Y-%m-%d %H:%M UTC")

          WHITE_SOURCES = [
              "WHITE-CIDR-RU-all.txt",
              "WHITE-CIDR-RU-checked.txt",
              "WHITE-SNI-RU-all.txt",
              "Vless-Reality-White-Lists-Rus-Mobile.txt",
              "Vless-Reality-White-Lists-Rus-Mobile-2.txt",
          ]
          BLACK_SOURCES = [
              "BLACK_VLESS_RUS.txt",
              "BLACK_VLESS_RUS_mobile.txt",
              "BLACK_SS+All_RUS.txt",
          ]

          PROTOCOLS = ("vless://", "vmess://", "trojan://", "ss://", "hysteria2://", "tuic://")

          MAX_PER_GROUP = 55
          PING_DOMAIN   = "telegram.org"

          def fetch(filename):
              try:
                  with urllib.request.urlopen(BASE + filename, timeout=20) as r:
                      return r.read().decode("utf-8").splitlines()
              except Exception as e:
                  print(f"  ERROR {filename}: {e}")
                  return []

          def is_vpn_line(line):
              return any(line.startswith(p) for p in PROTOCOLS)

          def is_anycast(line):
              name_part = line.split("#", 1)[1] if "#" in line else ""
              return "anycast" in name_part.lower()

          def get_ip(line):
              m = re.search(r'@([a-zA-Z0-9._-]+):', line)
              return m.group(1) if m else None

          def ping_via_telegram(host):
              try:
                  result = subprocess.run(
                      ["curl", "-o", "/dev/null", "-s", "-w", "%{time_total}",
                       "--max-time", "5", "--connect-timeout", "3",
                       "--resolve", f"{PING_DOMAIN}:443:{host}",
                       f"https://{PING_DOMAIN}"],
                      capture_output=True, text=True, timeout=10
                  )
                  val = result.stdout.strip()
                  if val:
                      return float(val) * 1000
              except:
                  pass
              try:
                  result = subprocess.run(["ping", "-c", "2", "-W", "2", host],
                                           capture_output=True, text=True, timeout=8)
                  for line in result.stdout.split("\n"):
                      if "avg" in line or "rtt" in line:
                          parts = line.split("/")
                          if len(parts) >= 5:
                              return float(parts[4])
              except:
                  pass
              return 9999.0

          def sort_by_ping(lines, max_check=60):
              candidates = lines[:max_check]
              scored = []
              for line in candidates:
                  host = get_ip(line)
                  p = ping_via_telegram(host) if host else 9999.0
                  print(f"    {host} -> {PING_DOMAIN}: {p:.0f}ms")
                  scored.append((p, line))
              scored.sort(key=lambda x: x[0])
              rest = lines[max_check:]
              return [l for _, l in scored] + rest

          def fetch_group(sources, is_white=False):
              all_lines, seen = [], set()
              for filename in sources:
                  print(f"  Fetching {filename}...")
                  for l in fetch(filename):
                      l = l.strip()
                      if not l or l.startswith("#") or not is_vpn_line(l):
                          continue
                      if is_anycast(l):
                          continue

                      base = re.sub(r'#.*$', '', l).strip()
                      if base in seen:
                          continue
                      seen.add(base)

                      if is_white:
                          if "#" in l:
                              prefix, name = l.split("#", 1)
                              l = f"{prefix}#⚪ {name}"
                          else:
                              l = l + "#⚪"

                      all_lines.append(l)
              return all_lines

          def write_file(path, header, white, black, fast):
              total = len(white) + len(black) + len(fast)
              with open(path, "w", encoding="utf-8") as f:
                  f.write(f"# {header}\n# Updated: {DATE}\n# Total: {total} servers\n\n")
                  if white:
                      f.write("# === WHITE SERVERS ===\n")
                      f.write("\n".join(white) + "\n\n")
                  if black:
                      f.write("# === BLACK SERVERS ===\n")
                      f.write("\n".join(black) + "\n\n")
                  if fast:
                      f.write("# === FAST ALL ===\n")
                      f.write("\n".join(fast) + "\n")

          print("Fetching WHITE sources...")
          white_raw = fetch_group(WHITE_SOURCES, is_white=True)
          print(f"  WHITE raw (after anycast filter): {len(white_raw)} keys")

          print("Fetching BLACK sources...")
          black_raw = fetch_group(BLACK_SOURCES, is_white=False)
          print(f"  BLACK raw (after anycast filter): {len(black_raw)} keys")

          print(f"Sorting WHITE by ping to {PING_DOMAIN} (top {MAX_PER_GROUP})...")
          white = sort_by_ping(white_raw, max_check=min(60, len(white_raw)))[:MAX_PER_GROUP]

          print(f"Sorting BLACK by ping to {PING_DOMAIN} (top {MAX_PER_GROUP})...")
          black = sort_by_ping(black_raw, max_check=min(60, len(black_raw)))[:MAX_PER_GROUP]

          print(f"Building FAST ALL group (top {MAX_PER_GROUP})...")
          seen_all, all_raw = set(), []
          for l in white_raw + black_raw:
              base = re.sub(r'#.*$', '', l).strip()
              if base not in seen_all:
                  seen_all.add(base)
                  all_raw.append(l)
          fast = sort_by_ping(all_raw, max_check=min(60, len(all_raw)))[:MAX_PER_GROUP]

          print("Writing files...")
          write_file("white_servers.txt", "WHITE SERVERS", white, [], [])
          write_file("black_servers.txt", "BLACK SERVERS", [], black, [])
          write_file("fast_servers.txt", "FAST ALL SERVERS", white, black, fast)

          print(f"\nDone! white={len(white)} black={len(black)} fast={len(fast)}")
          EOF

      - name: Commit changes
        run: |
          git config user.email "bot@vpnservice.local"
          git config user.name "VPN Sync Bot"
          git add white_servers.txt black_servers.txt fast_servers.txt
          git diff --staged --quiet || git commit -m "sync: $(date '+%Y-%m-%d %H:%M UTC')"
          git push
